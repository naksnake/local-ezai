{
  "mcpServers": {
    "filesystem": {
      "command": "mcp-server-filesystem",
      "args": ["/data/documents"],
      "description": "Read and write files in the project's documents folder"
    },
    "memory": {
      "command": "mcp-server-memory",
      "env": {
        "MEMORY_FILE_PATH": "/data/memory/memory.json"
      },
      "description": "Persistent knowledge graph memory across conversations"
    },
    "fetch": {
      "command": "mcp-server-fetch",
      "description": "Fetch and read any web page"
    },
    "qdrant-rag": {
      "command": "node",
      "args": ["/mcp-servers/qdrant-rag/index.js"],
      "env": {
        "QDRANT_URL": "http://qdrant:6333",
        "EMBED_URL": "http://embed-server:8001/v1",
        "COLLECTION": "${RAG_COLLECTION:-my-knowledge-base}"
      },
      "description": "Semantic search over your personal knowledge base"
    },
    "swe": {
      "command": "/opt/mcpo/bin/python",
      "args": ["/mcp-servers/swe-server/swe_server.py"],
      "env": {
        "EZAI_CONTROL_URL": "${EZAI_CONTROL_URL_MCPO:-http://ezaid:8010}",
        "EZAI_CONTROL_TOKEN": "${EZAI_CONTROL_TOKEN:-ezai-control-key}",
        "EZAI_ADMIN_URL": "http://${LAN_HOST:-localhost}:${MONITOR_PORT:-8888}"
      },
      "description": "Local-EZAI autonomous software engineering: plan and start work on registered projects, inspect runs, models and the governance queue (start + inspect only — no approvals from chat)"
    }
  }
}
